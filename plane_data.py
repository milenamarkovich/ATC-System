import math
import get_pos
from generate_planes import *

plane_data = {"spawn_pos": planeOrigin,
            "curr_pos": get_pos.getPosition(),
            "speed": v,
            "status": #landing or holding,
            "runway": 
            "safeZoneRadius": #eqn for circle of points 100m from other planes (can't overlap) - radius of circle = 50m}